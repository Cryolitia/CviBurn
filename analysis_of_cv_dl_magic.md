# `cv_dl_magic.S` 汇编解析

`cv_dl_magic.S` 是对 `cv_dl_magic.bin` 的源代码级重建。它生成的目标不是主机端 Linux 可执行文件，而是一个被 `usb_dl` / CviBurn 通过 USB 发送到目标 SoC 的 128 字节 AArch64 裸机 payload。这个 payload 运行在 CV180x/CV181x 的 ROM / 早期启动环境中，用于调整异常级、异常向量、协处理器访问和 MMU/cache 状态，然后把控制权交回 ROM 环境中的后续下载代码。

它不是普通函数，也不是 Linux 内核模块代码。它没有 ELF 入口、没有 C ABI、没有返回约定，也不应该在 x86_64 主机或 Linux 用户态/内核态中执行。主机端工具只把它当作字节序列发送给目标芯片。

## 为什么使用 `.inst`

源码中所有指令都用 `.inst` 写入，而不是直接写 AArch64 助记符：

```asm
.inst 0xd53e1100    /* mrs x0, SCR_EL3 */
```

这样做的目的不是为了可读性，而是为了保证构建结果与原始 `cv_dl_magic.bin` 字节级完全一致。这个 payload 只有 128 字节，并且存在跳转到自身范围之外的分支目标，例如 `0xa0`、`0x110`、`0x130`、`0x810` 和 `0x830`。这些地址并不在当前文件内部，而属于目标芯片 ROM 或早期启动上下文中的既有代码/数据区域。

如果改用标签、符号重定位或普通汇编伪指令，汇编器和链接器可能引入重定位、填充、对齐或不同的编码选择，从而破坏 payload 的原始字节布局。使用 `.inst` 可以精确指定每条 32 位 AArch64 指令的机器码，使最终通过 `objcopy -O binary` 导出的 `.text` section 恰好还原为原始的 128 字节 blob。

## 整体控制流

从偏移 `0x0000` 作为入口开始看，正常线性控制流非常短：

```text
0x0000 -> 0x0020 -> 0x0040 -> 0x00a0
```

也就是：

```asm
0000: b 0x20
0020: b 0x40
0040: mrs x0, SCR_EL3
...
0060: b 0xa0
```

因此，如果从 `cv_dl_magic_start` 入口执行，`0x0004` 到 `0x001c` 的代码块会被跳过，`0x0024` 到 `0x003c` 的代码块也会被跳过，`0x0064` 到 `0x007c` 的代码块同样不会被线性执行。

这并不代表这些代码一定“无用”。在 ROM 阶段 payload 中，经常存在多个潜在入口点，或者由 ROM 中其他代码跳入 blob 的中间偏移执行。由于这个文件本身不是完整程序，而是被放进特定 ROM 地址空间的一段小补丁/跳板代码，中间的代码块可能是给外部 ROM 逻辑调用的辅助入口。

从当前 0x80 字节本体能确定的是：正常入口路径主要执行 EL3 初始化，并跳转到 `0xa0` 处的外部 ROM/boot 代码；其余块更像是备用入口或被外部环境引用的片段。

## `0x0000`：跳过第一段辅助入口

```asm
/* 0000 */ .inst 0x14000008    /* b 0x20 */
```

AArch64 的 `b` 是无条件相对跳转。这里从 `0x0000` 跳到 `0x0020`，直接跳过 `0x0004` 到 `0x001c` 的代码。

这说明 `0x0004` 附近的代码不是正常入口的第一阶段，而更像是备用入口。正常从 blob 起始地址执行时，会继续跳到 `0x0040` 的 EL3 初始化逻辑。

## `0x0004` 到 `0x001c`：EL1 准备与外部 helper 调用

```asm
/* 0004 */ .inst 0xd518c000    /* msr    VBAR_EL1, x0 */
/* 0008 */ .inst 0xd2a00600    /* mov    x0, #0x300000 */
/* 000c */ .inst 0xd5181040    /* msr    CPACR_EL1, x0 */
/* 0010 */ .inst 0x94000040    /* bl     0x110 */
/* 0014 */ .inst 0x35000040    /* cbnz   w0, 0x1c */
/* 0018 */ .inst 0x14000000    /* b      0x18 */
/* 001c */ .inst 0x10003fa0    /* adr    x0, 0x810 */
```

这段代码首先写入 `VBAR_EL1`：

```asm
msr VBAR_EL1, x0
```

`VBAR_EL1` 是 EL1 异常向量基址寄存器。它决定 EL1 发生异常、中断、同步异常时跳转到哪一组 exception vector。这里使用调用者传入的 `x0` 作为向量表地址，说明这个辅助入口可能用于在 EL1 环境下安装或更新异常向量。

随后：

```asm
mov x0, #0x300000
msr CPACR_EL1, x0
```

`CPACR_EL1` 控制 EL1/EL0 对浮点、SIMD 等协处理器功能的访问。`0x300000` 对应设置 `FPEN` 位域为 `0b11`，通常表示允许 EL0/EL1 访问 FP/SIMD，不再因使用相关指令而 trap。对于早期 boot 代码来说，这一步可以避免后续 ROM 或解压/拷贝例程使用 FP/SIMD 相关状态时触发异常。

接着：

```asm
bl 0x110
```

跳转并链接到 `0x110`。这个目标超出了当前 0x80 字节 blob 的范围，因此它不是本文件内的函数，而是 ROM/早期启动环境中的外部代码。调用返回后检查 `w0`：

```asm
cbnz w0, 0x1c
b 0x18
```

如果 `w0 != 0`，说明外部 helper 返回成功，于是继续执行到 `0x001c`；如果 `w0 == 0`，则跳到自身：

```asm
b 0x18
```

这是一条死循环，相当于失败后停机等待。

`0x001c` 的：

```asm
adr x0, 0x810
```

把 PC-relative 地址 `0x810` 放入 `x0`。这个地址同样不在当前 blob 内。由于紧随其后的 `0x0020` 会跳到 `0x0040`，而 `0x0040` 又马上用 `mrs x0, SCR_EL3` 覆盖 `x0`，所以在从这段线性 fall-through 到 `0x0040` 的路径中，这个 `adr` 的值不会在当前 0x80 字节代码内被消费。

这提示我们：这里可能存在非线性的 ROM 调用约定，或者这段代码是从更大的原始 ROM patch 中截取出的片段。能确定的是：它引用了 `0x810` 这个外部地址，而这个地址的实际含义需要结合目标 ROM 内存布局才能解释。

## `0x0020`：跳到 EL3 初始化入口

```asm
/* 0020 */ .inst 0x14000008    /* b 0x40 */
```

这里从 `0x0020` 跳到 `0x0040`。因此正常入口：

```text
0x0000 -> 0x0020 -> 0x0040
```

前两段辅助代码都会被跳过，直接进入 EL3 控制寄存器配置。

## `0x0024` 到 `0x003c`：第二组 EL1 helper 入口

```asm
/* 0024 */ .inst 0xd518c000    /* msr    VBAR_EL1, x0 */
/* 0028 */ .inst 0xd2a00600    /* mov    x0, #0x300000 */
/* 002c */ .inst 0xd5181040    /* msr    CPACR_EL1, x0 */
/* 0030 */ .inst 0x94000040    /* bl     0x130 */
/* 0034 */ .inst 0x35000040    /* cbnz   w0, 0x3c */
/* 0038 */ .inst 0x14000000    /* b      0x38 */
/* 003c */ .inst 0x10003fa0    /* adr    x0, 0x830 */
```

这段与 `0x0004` 到 `0x001c` 的结构几乎完全相同：

```text
设置 VBAR_EL1
设置 CPACR_EL1 = 0x300000
调用外部 helper
若返回 0，则自旋
若返回非 0，则加载一个外部地址
```

区别在于：

```text
外部调用目标从 0x110 变成 0x130
外部地址从 0x810 变成 0x830
失败自旋点从 0x18 变成 0x38
```

这说明这两段很可能是两套相似的初始化/检测路径，分别对应 ROM 环境中的两个外部例程或两个不同数据结构。由于二者都设置 EL1 异常向量和 FP/SIMD 访问权限，它们可能服务于不同阶段的 EL1 代码入口，或者用于不同下载阶段的 trampoline。

同样，`0x003c` 的 `adr x0, 0x830` 在当前线性路径中也会被 `0x0040` 的 `mrs x0, SCR_EL3` 覆盖，因此不能仅凭本文件内的线性反汇编解释它的最终用途。

## `0x0040` 到 `0x0060`：EL3 初始化主路径

```asm
/* 0040 */ .inst 0xd53e1100    /* mrs    x0, SCR_EL3 */
/* 0044 */ .inst 0xb2400c00    /* orr    x0, x0, #0xf */
/* 0048 */ .inst 0xd51e1100    /* msr    SCR_EL3, x0 */
/* 004c */ .inst 0xd51e115f    /* msr    CPTR_EL3, xzr */
/* 0050 */ .inst 0xd53e1000    /* mrs    x0, SCTLR_EL3 */
/* 0054 */ .inst 0xd28000a1    /* mov    x1, #0x5 */
/* 0058 */ .inst 0x8a210000    /* bic    x0, x0, x1 */
/* 005c */ .inst 0xd51e1000    /* msr    SCTLR_EL3, x0 */
/* 0060 */ .inst 0x14000010    /* b      0xa0 */
```

这是正常入口最终会执行的核心部分。它运行在 EL3 语境下，直接读写 EL3 系统寄存器。

首先读取 `SCR_EL3`：

```asm
mrs x0, SCR_EL3
orr x0, x0, #0xf
msr SCR_EL3, x0
```

`SCR_EL3` 是 Secure Configuration Register。它控制 EL3 到低异常级的安全状态、异常路由和执行状态等行为。这里将低 4 位全部置 1：

```text
bit 0: NS
bit 1: IRQ
bit 2: FIQ
bit 3: EA
```

在常见 ARMv8-A 语义下，`NS` 影响较低异常级是否进入 Non-secure 状态；`IRQ`、`FIQ`、`EA` 影响 IRQ、FIQ、External Abort 等异常是否路由到 EL3。这里用 `orr #0xf` 保留原有高位配置，同时强制打开低 4 个控制位。对于 ROM 下载阶段来说，这通常意味着切换/准备后续非安全世界执行，并明确异常路由策略。

然后：

```asm
msr CPTR_EL3, xzr
```

`CPTR_EL3` 控制协处理器、trace、FP/SIMD 等功能是否 trap 到 EL3。写入 0 通常表示清除相关 trap 控制，让较低异常级或后续代码可以使用这些功能，而不被 EL3 捕获。结合前面对 `CPACR_EL1` 的设置，可以看出这个 payload 试图为后续执行环境放开 FP/SIMD/协处理器访问限制。

接着处理 `SCTLR_EL3`：

```asm
mrs x0, SCTLR_EL3
mov x1, #0x5
bic x0, x0, x1
msr SCTLR_EL3, x0
```

`SCTLR_EL3` 是 EL3 的系统控制寄存器。`bic x0, x0, #0x5` 清除 bit 0 和 bit 2：

```text
bit 0: M，MMU enable
bit 2: C，data/unified cache enable
```

也就是说，这段代码会关闭 EL3 下的 MMU 和数据 cache。对于早期 ROM 下载流程来说，这很合理：下载器往往需要在物理地址下拷贝 FIP、修改启动 flag、跳转到固定地址执行。关闭 MMU/cache 可以避免地址转换、cache coherency 或旧缓存内容干扰下载阶段。

最后：

```asm
b 0xa0
```

跳转到 `0xa0`，而 `0xa0` 超出了当前 0x80 字节 payload 的范围。这再次说明当前文件不是完整程序。`0xa0` 应该是目标 ROM/boot 环境中已有的后续入口点，或者是当前 payload 被放置到更大内存区域后，紧随其后的外部代码位置。

这条分支是正常入口路径的终点。也就是说，`0x0064` 之后的 EL2 代码不会在正常从 `0x0000` 开始的路径中执行。

## `0x0064` 到 `0x007c`：EL2 初始化辅助块

```asm
/* 0064 */ .inst 0xd51cc000    /* msr    VBAR_EL2, x0 */
/* 0068 */ .inst 0xd2867fe0    /* mov    x0, #0x33ff */
/* 006c */ .inst 0xd51c1140    /* msr    CPTR_EL2, x0 */
/* 0070 */ .inst 0xd53c1000    /* mrs    x0, SCTLR_EL2 */
/* 0074 */ .inst 0xd28000a1    /* mov    x1, #0x5 */
/* 0078 */ .inst 0x8a210000    /* bic    x0, x0, x1 */
/* 007c */ .inst 0xd51c1000    /* msr    SCTLR_EL2, x0 */
```

这一段配置 EL2：

```asm
msr VBAR_EL2, x0
```

设置 EL2 异常向量基址。和前面的 `VBAR_EL1` 类似，这说明调用者可能会以 `x0` 传入一组 EL2 exception vector 地址。

随后：

```asm
mov x0, #0x33ff
msr CPTR_EL2, x0
```

`CPTR_EL2` 控制 EL2 对协处理器、trace、FP/SIMD 等功能的 trap 行为。这里写入 `0x33ff`，说明它不是简单地完全清零，而是需要在 EL2 保持某种特定 trap/访问控制组合。具体每一位的含义取决于处理器实现和所启用的 ARMv8 扩展，但从整体意图看，它是在为后续 EL2/低异常级代码准备一个 ROM 期望的协处理器访问状态。

最后同样处理 `SCTLR_EL2`：

```asm
mrs x0, SCTLR_EL2
mov x1, #0x5
bic x0, x0, x1
msr SCTLR_EL2, x0
```

这会清除 EL2 的 bit 0 和 bit 2，也就是关闭 EL2 下的 MMU 和数据 cache。它与 EL3 主路径中清 `SCTLR_EL3` 的逻辑相对应，只是作用异常级从 EL3 换成 EL2。

需要注意的是，从 `cv_dl_magic_start` 正常进入时，执行会在 `0x0060` 跳到 `0xa0`，不会自然落入 `0x0064`。因此这段 EL2 代码应被视为另一个可能由外部 ROM 代码跳入的辅助入口，而不是主入口的一部分。

## 失败自旋逻辑

两段 EL1 helper 都有类似结构：

```asm
bl external_helper
cbnz w0, success
b .
```

例如：

```asm
0010: bl 0x110
0014: cbnz w0, 0x1c
0018: b 0x18
```

这表示外部 helper 返回 0 时，代码会进入永久自旋。这个行为在 ROM 阶段很常见：如果关键初始化或检查失败，系统没有操作系统、没有日志系统，也没有恢复路径，于是选择停在原地等待外部复位或调试。

从语义上看，`w0 != 0` 被视为成功，`w0 == 0` 被视为失败。

## 跳出 0x80 范围的目标

本文件大小只有 128 字节，即 `0x00` 到 `0x7f`。但代码中出现了多个超出范围的目标：

```text
0x0a0
0x110
0x130
0x810
0x830
```

这些目标不能按普通独立二进制解释。它们说明 `cv_dl_magic.bin` 被 ROM 放入某个特定地址后，是和目标芯片已有的 ROM/boot 代码共同工作的。

这些外部目标大致可以按用途分类：

```text
0x110 / 0x130:
    外部 helper 函数，通过 BL 调用，返回值放在 w0。

0x810 / 0x830:
    外部地址，通过 ADR 计算到 x0，可能是数据结构、向量表或后续代码入口。

0x0a0:
    主路径最终跳转目标，可能是 ROM 下载流程的继续执行位置。
```

因此，不能把这个 blob 当作自包含程序，也不能简单地“反编译成 C 函数”。它更接近一段 ROM 环境 patch / trampoline。

## 在 USB 下载流程中的作用

在 CviBurn/usb_dl 的下载流程中，主机首先连接到 CVITEK ROM USB 设备，然后发送这个 128 字节 payload。目标 ROM 接收后执行它，payload 完成特权级控制寄存器调整，并跳转回 ROM/早期启动环境中的后续下载逻辑。

结合后续协议行为看，它的作用可以概括为：

```text
1. 准备 EL3 / EL2 / EL1 的异常向量与控制寄存器状态。
2. 放开 FP/SIMD/协处理器访问限制。
3. 关闭早期阶段可能干扰物理地址下载的 MMU/cache。
4. 配置安全状态和异常路由。
5. 跳回 ROM 下载器的后续入口，让主机继续发送 fip.bin。
```

也就是说，它不是最终固件，也不是 U-Boot，更不是 Linux 代码。它只是 USB ROM 下载链路中的一个极短 trampoline，用来把目标芯片从初始 ROM USB 状态推进到能够继续接收和执行 FIP 下载流程的状态。

## 为什么不能在主机或 Linux 内核中执行

这段代码包含大量特权寄存器访问：

```asm
msr SCR_EL3, x0
msr CPTR_EL3, xzr
msr SCTLR_EL3, x0
msr VBAR_EL2, x0
msr CPTR_EL2, x0
msr SCTLR_EL2, x0
msr VBAR_EL1, x0
msr CPACR_EL1, x0
```

这些指令要求运行在正确的 ARM 异常级中。普通 Linux 用户态不能执行，Linux 内核模块通常也不能写 EL3 寄存器。更不用说主机端通常是 x86_64，根本不是 AArch64 指令集。

如果强行在错误环境执行，结果通常是：

```text
非法指令
同步异常
kernel oops / panic
跳转到未知地址
系统挂死
```

因此，CviBurn 的构建系统虽然从 `cv_dl_magic.S` 生成 `cv_dl_magic.bin`，但这个二进制只作为目标端 payload 被发送，不会被链接到主机程序，也不会被主机 CPU 执行。

## 小结

`cv_dl_magic.S` 是一个非常小但高度依赖上下文的 AArch64 ROM-stage payload。它的主路径从 `0x0000` 跳到 `0x0040`，配置 EL3 的 `SCR_EL3`、`CPTR_EL3`、`SCTLR_EL3`，然后跳转到外部 `0xa0` 继续执行。文件中还包含 EL1 和 EL2 相关的辅助入口，设置 `VBAR_EL1`、`CPACR_EL1`、`VBAR_EL2`、`CPTR_EL2` 和 `SCTLR_EL2`，但这些入口并非从正常起始地址线性执行，而应理解为 ROM 环境可能从外部跳入的代码片段。

它的核心目的不是实现复杂逻辑，而是在 ROM USB 下载早期建立一个适合继续下载 FIP 的处理器状态。由于它依赖目标 SoC 的 ROM 地址布局和异常级环境，必须保持字节级一致，不能随意重写、重排或“优化”。
